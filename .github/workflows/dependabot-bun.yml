name: Dependabot Bun Lockfile

on:
  pull_request:
    paths:
      - examples/hello/frontend/package.json

permissions:
  contents: write

jobs:
  update-lockfile:
    if: github.actor == 'dependabot[bot]'
    name: update bun.lock
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}

      - uses: oven-sh/setup-bun@v2

      - run: bun install
        working-directory: examples/hello/frontend

      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "🔒 update bun.lock"
          file_pattern: examples/hello/frontend/bun.lock
